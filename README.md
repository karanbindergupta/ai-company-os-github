# AI Company

Home for the AI Company Operating System.

**Status: preflight complete. The OS itself is not built yet.**

`.ai-company/tooling/` holds the 2026-09-07 environment audit — what this Claude Code
environment can do, what it cannot, what was installed, and what still needs the founder.
Read `SETUP-BLOCKERS.md` first.

This directory was created during the preflight because the audit ran in an ephemeral scratch
workspace that would have been destroyed at session end.
