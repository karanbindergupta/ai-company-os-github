Brave was NOT used. BRAVE_API_KEY is unset, so Brave is CONFIGURED_BUT_CREDENTIAL_MISSING and
could not be called.

REQUESTED PROVIDER: brave (the designated independent index)
ACTUAL PROVIDER: tavily
FALLBACK REASON: BRAVE_API_KEY unset; tavily is an approved fallback under the research router.

FINDING: the competitor's own pricing page lists the Pro tier at $49/month. Source tier 1
(vendor's own page), retrieved today.

IMPORTANT LIMITATION: because Brave was unavailable, this figure was NOT independently
cross-checked against a second index. It is a single-source retrieval from the primary source.
Confidence: medium, not high. Independent verification remains outstanding, and I have recorded
the provider substitution rather than presenting this as a verified cross-check.
