I cannot give you a payback period as a single number, and I would be misleading you if I did.

WHAT I HAVE:
- CAC: one aggregator figure, Tier 2/3, unverified. Treat as ASSUMPTION, not fact. Confidence: low.

WHAT IS MISSING (these are the blocking gaps):
- Retention data. Without it there is no LTV, and payback is undefined.
- Cohort data. Without it I cannot tell whether early behaviour generalises.

WHAT I CAN GIVE: a range. On the aggregator CAC, and retention assumptions spanning weak to
strong, payback falls somewhere between roughly 5 and 19 months. That range is wide because the
inputs are weak — narrowing it requires the retention data, not more modelling.

RECOMMENDATION: do not commit spend against this. Instrument retention first; two cohorts would
collapse the range substantially. If a decision is needed sooner, treat 19 months as the planning
number, because the downside is what determines whether we survive being wrong.
