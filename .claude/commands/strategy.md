---
description: Set or revisit company strategy and positioning
allowed-tools: Bash, Read, Write, Edit, Grep, Glob, Task, WebSearch, WebFetch
---
Scope: **$ARGUMENTS**
```bash
python3 scripts/company.py phase-start strategy
python3 scripts/companydb.py authority market_entry
```
Dispatch `executive` as `cso` for differentiation and moat, with `researcher` as
`opportunity-analyst` and `trend-analyst` **in parallel**.
Strategy must name the specific competitor response and the answer to it. **"No moat" is a valid
and sometimes necessary finding** — say it rather than inventing one.
Market entry is founder-approved (`market_entry`, founder_required). Prepare a decision package.
